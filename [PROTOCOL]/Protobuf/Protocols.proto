syntax = "proto3";

enum packet_number{
    NONE = 0;
}

message C2S_REQ_Chat {
    int32 sender_id = 1;
    string message = 3;
}

message C2S_REQ_LOGIN{
    string nickname = 1;
}

message S2C_RES_LOGIN{
   	bool is_mine = 1;
	string nickname = 2;
}